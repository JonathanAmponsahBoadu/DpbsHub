<?xml version="1.0" encoding="utf-8"?>
<!-- The notification icon is looked up by *name* at runtime, which resource
     shrinking can't see — without this a release build would strip it. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/ic_stat_dpbshub" />
